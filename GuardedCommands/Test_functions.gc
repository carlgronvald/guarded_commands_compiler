begin 
   x: int,
   function f(x: int): int = {
       print x;
       if x=0  -> return x + 1
           | x<>0 -> return x + 2
       fi
   }, 
   function g(x: int): int = {
       y: int;
       y := x;
       do ! y = 0  -> 
           print y; y := y - 1
       od;
       return y
   };
   print g(f(f(f(f(0)))))
end
